apiVersion: v1
kind: Service
metadata:
  name: search-platform-api
  labels:
    app.kubernetes.io/name: search-platform-api
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: search-platform-api
  ports:
    - name: http
      port: 80
      targetPort: http
