apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: search-platform-api
  labels:
    environment: production
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: search-platform-api-secrets
    creationPolicy: Owner
    template:
      metadata:
        labels:
          environment: production
      type: Opaque
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: /search-platform/production/database
        property: url
    - secretKey: OPENSEARCH_NODE
      remoteRef:
        key: /search-platform/production/opensearch
        property: endpoint
    - secretKey: OPENSEARCH_USERNAME
      remoteRef:
        key: /search-platform/production/opensearch
        property: username
    - secretKey: OPENSEARCH_PASSWORD
      remoteRef:
        key: /search-platform/production/opensearch
        property: password
    - secretKey: OTEL_EXPORTER_OTLP_ENDPOINT
      remoteRef:
        key: /search-platform/shared/observability
        property: otlpEndpoint
    - secretKey: OTEL_EXPORTER_OTLP_HEADERS
      remoteRef:
        key: /search-platform/shared/observability
        property: otlpHeaders
